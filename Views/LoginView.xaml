<UserControl x:Class="Project_BlueLock.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <Button Content="Go back" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Red" Height="45" Width="200" Command="{Binding GoToRegistration}"/>
            <Button Content="Go to CP" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Green" Margin="0 100 0 0" Height="45" Width="200" Command="{Binding GoToCreateProfile}"/>
            <Button Content="Go to Home" HorizontalAlignment="Center" VerticalAlignment="Center" Background="Blue" Margin="0 100 0 0" Height="45" Width="200" Command="{Binding GoToHome}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
