<UserControl x:Class="Project_BlueLock.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Project_BlueLock.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.1*"/>
            <ColumnDefinition Width="0.7*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.4*"/>
            <RowDefinition/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Viewbox Stretch="UniformToFill" Grid.ColumnSpan="3" Grid.RowSpan="3">
            <Image Source="/Assets/Images/login_page2.jpg"/>
        </Viewbox>

        <Border Grid.Column="1" Grid.Row="1" BorderBrush="#202020" BorderThickness="2" CornerRadius="40">
            <materialDesign:Card Background="#00020D" Opacity="0.78" >
                <StackPanel Orientation="Vertical">
                    <Viewbox HorizontalAlignment="Center" VerticalAlignment="Top">
                        <TextBlock Text="Log into your " Margin="-45 15 10 5" Style="{StaticResource KanitFont}" FontSize="10">
                            <Run Foreground="White">Log into your</Run>
                            <Run Foreground="#00E0FF">BlueLock</Run>
                            <Run Foreground="White">profile</Run>
                        </TextBlock>
                    </Viewbox>

                    <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 0 10">
                        <Grid>
                            <Border BorderBrush="#202020" BorderThickness="1" CornerRadius="10" Height="35" Width="300" Margin="20 5 20 0">
                                <TextBox Background="Transparent" Foreground="White" FontSize="10" VerticalAlignment="Top" Height="35" Width="260" Margin="12 7 26 0" materialDesign:HintAssist.Hint="Username">
                                    <TextBox.Text>
                                        <Binding Path="Text2" UpdateSourceTrigger="PropertyChanged"></Binding>
                                    </TextBox.Text>
                                </TextBox>
                            </Border>
                        </Grid>
                    </Viewbox>

                    <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 0 10">
                        <Grid>
                            <Border BorderBrush="#202020" BorderThickness="1" CornerRadius="10" Height="35" Width="300" Margin="20 5 20 0">
                                <PasswordBox Background="Transparent" Foreground="White" FontSize="10" VerticalAlignment="Top" Height="35" Width="260" Margin="12 7 26 0" materialDesign:HintAssist.Hint="Password" materialDesign:TextFieldAssist.HasClearButton="True"/>
                            </Border>
                        </Grid>
                    </Viewbox>

                    <Button Width="360"
                            Height="80" 
                            Margin="0 15 0 0" 
                            materialDesign:ButtonAssist.CornerRadius="14" 
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            Command="{Binding GoToLogin}">
                        <TextBlock Text="Login" HorizontalAlignment="Center" VerticalAlignment="Bottom" Style="{StaticResource KanitFont}" Foreground="White" FontSize="24"/>
                    </Button>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 -10 0 0">
                        <TextBlock Text="Don't have an account?" Foreground="White" FontSize="16" />
                        <TextBlock Text="Create profile" Margin="6 0 0 0" FontSize="16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <EventSetter Event="MouseLeftButtonDown" Handler="CreateProfile_Click" />
                                    <Setter Property="Foreground" Value="Blue" />
                                    <Setter Property="TextDecorations" Value="Underline" />
                                    <Setter Property="Cursor" Value="Hand" />
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Border>
    </Grid>
</UserControl>
